# Oracle v2 Environment Variables
# Copy to .env and fill in values

# Server
PORT=47778
ORACLE_SESSION_SECRET=           # Random string for session signing (auto-generated if empty)
CORS_ORIGIN=                     # Allowed CORS origin for production (e.g., https://yourdomain.com)

# Database
ORACLE_DATA_DIR=                 # Path to Oracle data directory (default: ~/.oracle)
# Managed Postgres (fleet-pg / oracle_kb) — used by the pgvector adapter. For DO
# Managed PG keep sslmode in the URL AND set the CA below: the URL's sslmode is
# stripped and ssl:{ca} is used instead (avoids "self signed certificate in chain").
ORACLE_DATABASE_URL=             # e.g. postgres://user:pass@host:25060/oracle_kb?sslmode=require
DATABASE_CA_CERT=                # PEM CA cert for managed PG TLS (multiline)

# Vector Store (choose one): chroma | sqlite-vec | lancedb | qdrant | cloudflare-vectorize | pgvector
ORACLE_VECTOR_DB=lancedb
CHROMA_URL=http://localhost:8000
QDRANT_URL=http://localhost:6333
QDRANT_API_KEY=

# Embeddings
OPENAI_API_KEY=                  # For OpenAI embeddings
OLLAMA_BASE_URL=http://localhost:11434  # For local Ollama embeddings

# Cloudflare (optional)
CLOUDFLARE_ACCOUNT_ID=
CLOUDFLARE_API_TOKEN=
ORACLE_EMBEDDING_MODEL=

# Forum (optional)
ORACLE_FORUM_REPO=               # GitHub repo for forum sync (e.g., owner/repo)

# Source mappings (optional JSON)
ORACLE_SOURCE_MAPPINGS=          # e.g., {"key":"github.com/owner/repo"}
